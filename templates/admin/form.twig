{% extends 'admin/layout.twig' %}

{% block title %}
    {{ title }}
{% endblock %}


{% block content %}
<div class="col-lg-12">
    <h1 class="text-primary">{{ title }}</h1>
</div>
<div class="col-lg-12  well">
    {{ form(form) }}
</div>

{% endblock %}
